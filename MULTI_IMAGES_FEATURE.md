# ميزة الصور المتعددة للأخبار 📸

## نظرة عامة
تم تطوير نظام متكامل لرفع وإدارة صور متعددة (حتى 10 صور) لكل خبر مع عرضها في Carousel تفاعلي.

## الميزات ✨

### 1. رفع صور متعددة
- رفع حتى **10 صور** لكل خبر
- دعم الصيغ: JPG, JPEG, PNG, GIF, WEBP
- حد أقصى لكل صورة: **5MB**
- معاينة مباشرة للصور قبل الرفع
- إمكانية حذف الصور قبل النشر

### 2. تنظيم المجلدات في Cloudinary
```
quranic-school/
  └── news/
      └── {teacherName}_{teacherId}/
          └── {newsId}/
              ├── image_1732465789123_abc123.jpg
              ├── image_1732465789456_def456.jpg
              └── ...
```

### 3. عرض الصور (Carousel)
- تنقل سلس بين الصور باستخدام الأسهم
- عداد للصور (1/5 مثلاً)
- مؤشرات (dots) أسفل الصورة
- عرض تلقائي للصورة الأولى
- دعم الأخبار القديمة (صورة واحدة)

### 4. إدارة ذكية للصور
- **عند التعديل**: حذف الصور القديمة تلقائياً ورفع الجديدة
- **عند الحذف**: حذف مجلد الخبر بالكامل من Cloudinary
- **التوافق العكسي**: الأخبار القديمة بصورة واحدة تعمل بدون مشاكل

## التغييرات التقنية 🔧

### Backend
1. **Schema (News.js)**
   - إضافة حقل `images: [{url, publicId}]`
   - الحفاظ على `image` و `imagePublicId` للتوافق

2. **Multer Configuration**
   - تحديث لدعم `.array("images", 10)`
   - هيكلة مجلدات منظمة

3. **Controllers**
   - `createNews`: رفع صور متعددة
   - `updateNews`: حذف الصور القديمة + رفع الجديدة
   - `deleteNews`: حذف المجلد الكامل

### Frontend
1. **MultiImageUpload Component** (جديد)
   - مكون متخصص لرفع صور متعددة
   - معاينة Grid responsive
   - ترقيم الصور

2. **NewsCard with Carousel**
   - أزرار تنقل (السابق/التالي)
   - عداد الصور
   - مؤشرات تفاعلية

3. **NewsModal**
   - استخدام MultiImageUpload
   - عرض الصور الموجودة عند التعديل

## الاستخدام 📖

### إضافة خبر بصور متعددة
1. افتح صفحة الأخبار
2. اضغط "إضافة خبر جديد"
3. املأ العنوان والمحتوى
4. اضغط على منطقة رفع الصور
5. اختر حتى 10 صور
6. معاينة الصور وحذف أي صورة غير مرغوبة
7. احفظ الخبر

### تعديل خبر
- الصور الحالية تظهر تلقائياً
- يمكنك اختيار صور جديدة (ستحل محل القديمة)
- عند الحفظ: حذف القديم + رفع الجديد

### عرض الأخبار
- إذا كان الخبر يحتوي على صورة واحدة: عرض عادي
- إذا كان يحتوي على صور متعددة:
  - أسهم للتنقل
  - عداد (1/5)
  - نقاط للانتقال السريع

## الصلاحيات 🔐

- **المعلم**: يمكنه رفع صور لأخباره فقط
- **الأدمن**: يمكنه رفع صور لجميع الأخبار
- كل معلم له مجلد خاص به

## التحديثات المستقبلية المقترحة 🚀

- [ ] إضافة خاصية السحب والإفلات للصور
- [ ] ترتيب الصور يدوياً
- [ ] ضغط الصور قبل الرفع
- [ ] إضافة تأثيرات انتقال للـ Carousel
- [ ] دعم الفيديوهات

## الملاحظات الهامة ⚠️

1. **حجم الصور**: تأكد أن كل صورة أقل من 5MB
2. **التحسين التلقائي**: Cloudinary يحسن الصور تلقائياً
3. **الحذف الآمن**: عند حذف خبر، يُحذف المجلد كاملاً
4. **الأداء**: استخدام lazy loading للصور

---

تم التطوير: نوفمبر 2025
