# تحديث بنية الحلقات للمعلمين (Groups Structure Update)

## نظرة عامة

تم تحديث بنية حقل `groups` في نموذج المعلم من مصفوفة نصوص بسيطة إلى مصفوفة كائنات تحتوي على معلومات مفصلة عن كل حلقة.

## التغييرات المطبقة

### 1. Backend Changes

#### Models (نماذج قاعدة البيانات)
- **Teacher.js**: تم إنشاء `groupSubSchema` جديد يحتوي على:
  - `id`: معرف الحلقة (ObjectId مرتبط بنموذج Group)
  - `name`: اسم الحلقة
  - `number`: رقم الحلقة

#### Controllers (وحدات التحكم)
- **teacherController.js**: تم تحديث وظائف الإنشاء والتحديث لدعم البنية الجديدة والقديمة
- **authController.js**: متوافق مع البنية الجديدة

#### Routes (المسارات)
- **teacherRoutes.js**: تم تحديث البحث ليدعم البنية الجديدة والقديمة

### 2. Frontend Changes

#### Types & Interfaces
- **teacherApi.ts**: تم تحديث interface Teacher لاستخدام البنية الجديدة
- **teacherValidation.ts**: تم تحديث validation schema للحلقات

#### Components
- **AddTeacherForm.tsx**: تم تحديث منطق التعامل مع الحلقات
- **AddStudentForm.tsx**: تم تحديث البحث عن المعلمين بناءً على الحلقات

## البنية الجديدة

### قبل التحديث (البنية القديمة)
```javascript
groups: ["حلقة تحفيظ القرآن الأولى", "حلقة تحفيظ القرآن الثانية"]
```

### بعد التحديث (البنية الجديدة)
```javascript
groups: [
  {
    id: ObjectId("..."),
    name: "حلقة تحفيظ القرآن الأولى",
    number: 1
  },
  {
    id: ObjectId("..."), 
    name: "حلقة تحفيظ القرآن الثانية",
    number: 2
  }
]
```

## Migration Script (سكريبت التحويل)

تم إنشاء سكريبت لتحويل البيانات القديمة إلى البنية الجديدة:

```bash
cd Backend/src
node scripts/migrateTeacherGroups.js
```

هذا السكريبت سيقوم بـ:
- البحث عن جميع المعلمين الذين لديهم حلقات بالبنية القديمة
- تحويل أسماء الحلقات إلى كائنات مع معرفات من نموذج Group
- إنشاء حلقات جديدة إذا لم توجد في قاعدة البيانات
- تحديث بيانات المعلمين بالبنية الجديدة

## التوافق مع البيانات القديمة

تم تطبيق التوافق العكسي في:

1. **Backend Controllers**: تتعامل مع البنية القديمة والجديدة
2. **Routes**: البحث يدعم كلا البنيتين
3. **Frontend Components**: التحقق من نوع البيانات قبل المعالجة

## فوائد التحديث

1. **معلومات أكثر تفصيلاً**: كل حلقة لها معرف ورقم منفصل
2. **ربط أفضل**: ربط مباشر مع نموذج Group
3. **مرونة أكبر**: إمكانية إضافة خصائص جديدة للحلقات لاحقاً
4. **تنظيم أفضل**: ترقيم وتصنيف الحلقات

## ملاحظات للمطورين

- استخدم دائماً البنية الجديدة عند إنشاء بيانات جديدة
- السكريبت آمن للتشغيل مرات متعددة
- تأكد من backup قاعدة البيانات قبل تشغيل migration
- التحديثات متوافقة مع البيانات الموجودة

## أمثلة الاستخدام

### إنشاء معلم جديد
```javascript
const teacherData = {
  firstName: "أحمد",
  lastName: "محمد",
  groups: [
    {
      id: "60f7b3b3b3b3b3b3b3b3b3b3",
      name: "حلقة القرآن المتقدمة",
      number: 1
    }
  ]
  // باقي البيانات...
};
```

### البحث عن معلم بالحلقة
```javascript
// يدعم البنية القديمة والجديدة
const teachers = await Teacher.find({
  $or: [
    { 'groups.name': groupName },
    { 'groups.id': groupId },
    { groups: groupName } // دعم البيانات القديمة
  ]
});
```