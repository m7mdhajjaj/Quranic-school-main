# نظام الترتيب المزدوج 🏆

## نظرة عامة

تم إضافة نظامين للترتيب في صفحة "لعبة النقاط اليومية":

1. **ترتيب النقاط** 📊 - حسب النقاط المكتسبة
2. **ترتيب الشارات** 🏆 - حسب عدد الشارات ومجموع التكرارات

---

## الميزات الجديدة ✨

### 1. زر التبديل بين الترتيبين

في نافذة "لوحة الترتيب"، يوجد زران للتبديل:

- **📊 ترتيب النقاط** - يعرض أفضل 10 طلاب حسب النقاط
- **🏆 ترتيب الشارات** - يعرض أفضل 10 طلاب حسب الشارات

### 2. عرض ديناميكي للمعلومات

- **في ترتيب النقاط:**
  - يعرض النقاط بخط كبير
  - "نقطة" تحت الرقم
- **في ترتيب الشارات:**
  - يعرض مجموع التكرارات بخط كبير
  - "تكرار" تحت الرقم
  - عدد الشارات المكتسبة (🏆 X)
  - معلومات إضافية: "X شارات • Y تكرار إجمالي"

### 3. أيقونات مميزة

- ترتيب النقاط: 🏆
- ترتيب الشارات: 🎖️

---

## كيفية الاستخدام 📖

### للطالب:

1. اضغط على زر **"🏅 لوحة الترتيب"** في الصفحة الرئيسية
2. تُفتح نافذة الترتيب (افتراضياً: ترتيب النقاط)
3. اضغط على **"🏆 ترتيب الشارات"** لمشاهدة الترتيب حسب الشارات
4. اضغط على **"📊 ترتيب النقاط"** للعودة لترتيب النقاط

---

## آلية الترتيب 🔢

### ترتيب النقاط 📊

```typescript
// الترتيب حسب:
1. النقاط (من الأعلى للأدنى)

// مثال:
الطالب 1: 985 نقطة → المركز الأول
الطالب 2: 920 نقطة → المركز الثاني
الطالب 3: 895 نقطة → المركز الثالث
```

### ترتيب الشارات 🏆

```typescript
// الترتيب حسب:
1. مجموع تكرارات الشارات (من الأعلى للأدنى)
2. عدد الشارات (في حالة التساوي)

// حساب مجموع التكرارات:
totalBadgeRepeats = sum of all badge.count

// مثال:
الطالب عنده:
- المصلي المجتهد: count = 3
- نجم الأذكار: count = 5
- بار بوالديه: count = 2
مجموع التكرارات = 3 + 5 + 2 = 10 تكرار
```

---

## البيانات المعروضة 📊

### في ترتيب النقاط:

```tsx
{
  rank: number,           // الترتيب (1-10)
  name: string,           // اسم الطالب
  points: number,         // النقاط
  emoji: string,          // الإيموجي المميز
  badgesCount: number,    // عدد الشارات (لا يُعرض لكن موجود)
  totalBadgeRepeats: number  // مجموع التكرارات (لا يُعرض لكن موجود)
}
```

### في ترتيب الشارات:

```tsx
{
  rank: number,           // الترتيب (1-10)
  name: string,           // اسم الطالب
  badgesCount: number,    // عدد الشارات ✅ يُعرض
  totalBadgeRepeats: number,  // مجموع التكرارات ✅ يُعرض
  emoji: string,          // الإيموجي المميز
  points: number          // النقاط (لا يُعرض لكن موجود)
}
```

---

## التصميم 🎨

### الألوان والأيقونات حسب المركز:

| المركز        | الخلفية                         | الأيقونة     |
| ------------- | ------------------------------- | ------------ |
| الأول (🥇)    | `from-yellow-300 to-yellow-500` | 🥇 / 👑      |
| الثاني (🥈)   | `from-gray-300 to-gray-400`     | 🥈 / 🏆      |
| الثالث (🥉)   | `from-orange-300 to-orange-500` | 🥉 / 🥇      |
| 4-10          | `bg-gray-300`                   | ⭐💫✨⚡🔥💪 |
| الطالب الحالي | `from-blue-500 to-purple-600`   | 🌟           |

### أزرار التبديل:

```tsx
// الزر النشط:
className = "bg-white text-orange-600 shadow-lg scale-105";

// الزر غير النشط:
className = "bg-white/20 text-white hover:bg-white/30";
```

---

## أمثلة عملية 💡

### مثال 1: طالب في المركز الخامس

**ترتيب النقاط:**

```
المركز: 5
الاسم: أحمد محمد
النقاط: 780 نقطة
```

**ترتيب الشارات:**

```
المركز: 7
الاسم: أحمد محمد
الشارات: 5 شارات
التكرارات: 12 تكرار إجمالي
```

### مثال 2: طالب حاصل على 3 شارات

**شاراته:**

- 🕌 المصلي المجتهد: count = 2 (حصل عليها مرتين)
- ⭐ نجم الأذكار: count = 1
- ❤️ بار بوالديه: count = 3 (حصل عليها 3 مرات!)

**في ترتيب الشارات:**

- عدد الشارات: 3 🏆
- مجموع التكرارات: 2 + 1 + 3 = **6 تكرار**
- النص المعروض: "3 شارات • 6 تكرار إجمالي"

---

## الفرق بين النظامين 🆚

| الميزة       | ترتيب النقاط   | ترتيب الشارات           |
| ------------ | -------------- | ----------------------- |
| **المعيار**  | النقاط اليومية | مجموع تكرارات الشارات   |
| **التركيز**  | الأداء الحالي  | الإنجاز طويل المدى      |
| **التحفيز**  | جمع نقاط يومية | جمع شارات وتكرارها      |
| **العرض**    | رقم النقاط     | التكرارات + عدد الشارات |
| **الأيقونة** | 🏆             | 🎖️                      |

---

## لماذا نظامان؟ 🤔

### ترتيب النقاط 📊

**يُظهر:**

- الأداء الحالي (هذا الشهر)
- النشاط اليومي
- من يجتهد الآن

**مفيد لـ:**

- تحفيز المنافسة اليومية
- مكافأة الجهد المستمر
- تتبع التقدم قصير المدى

### ترتيب الشارات 🏆

**يُظهر:**

- الإنجاز التراكمي
- الالتزام طويل المدى
- من حقق أهداف كبيرة

**مفيد لـ:**

- تقدير الجهد المستمر
- تكريم الطلاب المثابرين
- تشجيع الأهداف طويلة المدى

---

## التحديثات التلقائية 🔄

### للطالب الحالي:

```typescript
// في ترتيب الشارات، بيانات الطالب الحالي تُحدّث تلقائياً:
{
  badgesCount: earnedBadges.length,  // عدد الشارات الحقيقي
  totalBadgeRepeats: earnedBadges.reduce((sum, b) => sum + b.count, 0)  // المجموع الحقيقي
}
```

### للطلاب الآخرين:

حالياً: بيانات وهمية (mock data)
مستقبلاً: سيتم ربطها بـ API لجلب البيانات الحقيقية

---

## الكود 💻

### State للتبديل:

```typescript
const [rankingType, setRankingType] = useState<"points" | "badges">("points");
```

### البيانات:

```typescript
// ترتيب النقاط
const mockRankings = [
  {
    rank: 1,
    name: "محمد أحمد",
    points: 985,
    badgesCount: 8,
    totalBadgeRepeats: 25,
    emoji: "🥇",
  },
  // ...
];

// ترتيب الشارات (مرتب حسب totalBadgeRepeats)
const mockBadgeRankings = [
  {
    rank: 1,
    name: "محمد أحمد",
    badgesCount: 8,
    totalBadgeRepeats: 25,
    points: 985,
    emoji: "👑",
  },
  // ...
];
```

### العرض الديناميكي:

```tsx
{(rankingType === "points" ? mockRankings : mockBadgeRankings).map((student) => (
  // عرض الطالب...
))}
```

---

## التطوير المستقبلي 🚀

### 1. API Integration

```typescript
// جلب البيانات من Backend
const fetchRankings = async (type: "points" | "badges") => {
  const response = await fetch(`/api/rankings/${type}`);
  return response.json();
};
```

### 2. فلاتر إضافية

- ترتيب أسبوعي / شهري / سنوي
- ترتيب حسب المجموعة
- ترتيب حسب الصف

### 3. إحصائيات متقدمة

- متوسط النقاط
- أكثر شارة مكتسبة
- معدل النمو

### 4. رسوم بيانية

- خط الزمن للترتيب
- مقارنة مع الأسبوع السابق
- توزيع الشارات

---

## الخلاصة ✅

**تم إضافة:**

- ✅ نظام ترتيب مزدوج (نقاط + شارات)
- ✅ أزرار تبديل سلسة
- ✅ عرض ديناميكي للمعلومات
- ✅ تحديث تلقائي لبيانات الطالب الحالي
- ✅ تصميم جذاب ومتناسق
- ✅ رسائل تحفيزية مخصصة

**الهدف:**
تحفيز الطلاب من خلال عرض تقدمهم من زاويتين:

1. الأداء اليومي (النقاط)
2. الإنجاز طويل المدى (الشارات)

---

تم التطوير بواسطة: GitHub Copilot 🤖✨  
التاريخ: 17 أكتوبر 2025  
الحالة: ✅ مكتمل وجاهز للاستخدام
