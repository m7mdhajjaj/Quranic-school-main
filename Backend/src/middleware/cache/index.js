/**
 * ============================================================================
 * Cache Middleware Index
 * ============================================================================
 */

const { cacheMiddleware, invalidateCache } = require('./cache.middleware');

module.exports = {
  cacheMiddleware,
  invalidateCache,
};
