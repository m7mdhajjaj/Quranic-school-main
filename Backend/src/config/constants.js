// ============================================================================
// config/constants.js - الثوابت القابلة للتخصيص
// ============================================================================
// 
// هذا الملف يحتوي على جميع الثوابت القابلة للتعديل في النظام
// لتسهيل التخصيص بدون تعديل الكود
//
// ============================================================================

/**
 * ============================================================================
 * 📝 إعدادات العلامات اليومية (DailyMark)
 * ============================================================================
 */

/**
 * مدة نافذة التعديل بعد تاريخ المقطع (بالأيام)
 * بعد هذه المدة، لا يمكن إضافة أو تعديل العلامات
 * @type {number}
 */
const EDIT_WINDOW_DAYS = 14; // أسبوعين

/**
 * الحصة الأسبوعية للتسميع (عدد المقاطع في الأسبوع)
 * @type {number}
 */
const WEEKLY_QUOTA = 3;

/**
 * الحد الأدنى لحجم المقطع (عدد الآيات)
 * @type {number}
 */
const MIN_CHUNK_SIZE = 5;

/**
 * الحجم الافتراضي الأدنى للمقطع (عدد الآيات)
 * @type {number}
 */
const DEFAULT_CHUNK_MIN = 10;

/**
 * الحجم الافتراضي الأقصى للمقطع (عدد الآيات)
 * @type {number}
 */
const DEFAULT_CHUNK_MAX = 15;

/**
 * ============================================================================
 * 📅 إعدادات الجدول الزمني (Timetable)
 * ============================================================================
 */

/**
 * الحد الأدنى لمدة الحلقة بالدقائق
 * @type {number}
 */
const MIN_SESSION_DURATION = 60;

/**
 * الحد الأقصى لمدة الحلقة بالدقائق
 * @type {number}
 */
const MAX_SESSION_DURATION = 150; // ساعتين ونص

/**
 * ============================================================================
 * تصدير الثوابت
 * ============================================================================
 */

module.exports = {
  // DailyMark
  EDIT_WINDOW_DAYS,
  WEEKLY_QUOTA,
  MIN_CHUNK_SIZE,
  DEFAULT_CHUNK_MIN,
  DEFAULT_CHUNK_MAX,
  
  // Timetable
  MIN_SESSION_DURATION,
  MAX_SESSION_DURATION,
};
