# 📬 توثيق نظام الإشعارات - Quranic School

## 📋 نظرة عامة

نظام الإشعارات يدعم **30+ نوع** من الإشعارات مقسمة إلى **4 فئات** رئيسية:

| الفئة | الوصف | الألوان |
|-------|-------|---------|
| 🔔 **عام (General)** | إشعارات عامة، رسائل، أخبار | Slate, Blue, Cyan |
| 📚 **أكاديمي (Academic)** | حضور، علامات، جدول، قرآن | Emerald, Teal, Amber |
| ⚙️ **إداري (Admin)** | إجراءات الأدمن، تعيينات، تحديثات | Blue, Purple, Rose |
| ⭐ **أخرى (Other)** | صلاة، أهداف، إنجازات، نقاط | Purple, Yellow, Orange |

---

## 🔔 الفئة العامة (General)

### 1. `message` - رسالة جديدة
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند استلام رسالة جديدة في المحادثة |
| **المستلم** | الشخص المُرسل إليه الرسالة |
| **Handler** | `ChatHandler.notifyNewMessage()` |
| **العنوان** | `اسم المرسل` أو `اسم المرسل - اسم المجموعة` |
| **الرسالة** | نص الرسالة (أول 50 حرف) |

```javascript
// مثال الاستخدام
ChatHandler.notifyNewMessage(recipientId, 'Student', 'أحمد', 'السلام عليكم...', conversationId, 'DM');
```

---

### 2. `mention` - إشارة (@)
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند الإشارة لشخص بـ @ في المحادثة |
| **المستلم** | الشخص المُشار إليه |
| **Handler** | `ChatHandler.notifyMention()` |
| **العنوان** | `إشارة من اسم_المرسل` |

| **الرسالة** | نص الرسالة |

---

### 3. `news` - منشور/خبر جديد
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند إضافة منشور جديد |
| **المستلم** | جميع الطلاب والمعلمين (عام) أو طلاب المعلم (خاص) |
| **Handler** | `NewsHandler.notifyNewsCreated()` |
| **العنوان** | `منشور جديد` |
| **الرسالة** | عنوان المنشور |

```javascript
// الأخبار العامة → جميع الطلاب + المعلمين
// أخبار الحلقة → طلاب المعلم فقط
```

---

### 4. `news` (تحديث) - تعديل منشور
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تعديل منشور موجود |
| **المستلم** | نفس مستلمي الإنشاء |
| **Handler** | `NewsHandler.notifyNewsUpdated()` |
| **العنوان** | `تحديث منشور` |

---

### 5. `system` - إشعار نظام
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | رسائل النظام العامة |
| **Handler** | `GeneralHandler.notifySystemMessage()` |

---

### 6. `warning` - تحذير عام
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | تحذيرات النظام |
| **Handler** | `GeneralHandler.notifyWarning()` |

---

## 📚 الفئة الأكاديمية (Academic)

### 7. `attendance` - غياب
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند رصد غياب الطالب |
| **المستلم** | الطالب الغائب |
| **Handler** | `AttendanceHandler.notifyAbsence()` |
| **العنوان** | `غياب` |
| **الرسالة** | `تم رصد غيابك بتاريخ {التاريخ}` |
| **الأولوية** | `high` |

```javascript
// يُستدعى من AttendanceController عند:
// - رصد حضور → student.isAbsent = true
// - رصد حضور جماعي للغائبين
```

---

### 8. `attendance` (تعديل) - تعديل حضور
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تحويل غياب إلى حضور |
| **المستلم** | الطالب |
| **Handler** | `AttendanceHandler.notifyAbsenceRemoved()` |
| **العنوان** | `تعديل حضور` |
| **الرسالة** | `تم تعديلك لحاضر بتاريخ {التاريخ}` |

---

### 9. `daily_marks` - مقطع جديد
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند إضافة مقطع يومي جديد للحلقة |
| **المستلم** | جميع طلاب الحلقة |
| **Handler** | `DailyMarksHandler.notifySectionAdded()` |
| **العنوان** | `مقطع جديد` |
| **الرسالة** | `تم إضافة مقطع بتاريخ {التاريخ}` |
| **data.action** | `section_added` |

```javascript
// يُستدعى من DailyMarkController عند:
// - POST /api/daily-marks/section
```

---

### 10. `daily_marks` (تحديث) - تعديل مقطع
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تعديل مقطع موجود |
| **المستلم** | جميع طلاب الحلقة |
| **Handler** | `DailyMarksHandler.notifySectionUpdated()` |
| **العنوان** | `تحديث مقطع` |
| **data.action** | `section_updated` |

---

### 11. `daily_marks` (حذف) - حذف مقطع
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند حذف مقطع |
| **المستلم** | جميع طلاب الحلقة |
| **Handler** | `DailyMarksHandler.notifySectionDeleted()` |
| **العنوان** | `حذف مقطع` |
| **data.action** | `section_deleted` |

---

### 12. `timetable` - موعد جديد
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند إضافة موعد جديد للحلقة |
| **المستلم** | جميع طلاب الحلقة |
| **Handler** | `TimetableHandler.notifyTimetableCreated()` |
| **العنوان** | `موعد جديد` |
| **الرسالة** | `موعد {نوع_الحلقة} - {التاريخ} ({الوقت})` |

```javascript
// أنواع الحلقة:
// - hifz: حفظ
// - murajaah: مراجعة  
// - both: حفظ ومراجعة
```

---

### 13. `timetable` (تعديل) - تعديل موعد
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تعديل موعد موجود |
| **المستلم** | جميع طلاب الحلقة |
| **Handler** | `TimetableHandler.notifyTimetableUpdated()` |
| **العنوان** | `تعديل موعد` |

---

### 14. `timetable` (إلغاء) - إلغاء موعد
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند حذف/إلغاء موعد |
| **المستلم** | جميع طلاب الحلقة |
| **Handler** | `TimetableHandler.notifyTimetableDeleted()` |
| **العنوان** | `إلغاء موعد` |

---

### 15. `student_update` - طالب جديد
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند إضافة طالب للحلقة |
| **المستلم** | معلم الحلقة |
| **Handler** | `StudentHandler.notifyStudentAddedToGroup()` |
| **العنوان** | `طالب جديد` |
| **الرسالة** | `طالب جديد: {اسم الطالب}` |

```javascript
// يُستدعى فقط عند الإضافة بواسطة الأدمن/السكرتير
// لا يُستدعى عند التسجيل الذاتي
```

---

### 16. `student_update` (حذف) - حذف طالب
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند إزالة طالب من الحلقة |
| **المستلم** | معلم الحلقة |
| **Handler** | `StudentHandler.notifyStudentRemovedFromGroup()` |
| **العنوان** | `حذف طالب` |

---

### 17. `student_update` (نقل) - نقل طالب
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند نقل طالب بين حلقتين |
| **المستلم** | معلم الحلقة القديمة + معلم الحلقة الجديدة |
| **Handler** | `StudentHandler.notifyStudentMovedGroup()` |
| **العنوان** | `نقل طالب` (للقديم) / `طالب جديد` (للجديد) |

---

### 18. `warning` - تنبيه/إنذار طالب
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند إصدار تنبيه أو إنذار للطالب |
| **المستلم** | الطالب |
| **Handler** | `WarningHandler.notifyStudentWarning()` |

#### أنواع التنبيهات:

| النوع | العنوان | الأولوية |
|-------|---------|----------|
| `warning` | تنبيه | medium |
| `first` | إنذار أول | high |
| `second` | إنذار ثاني | high |
| `third` | إنذار نهائي | urgent |
| `expulsion` | قرار فصل | urgent |

```javascript
// مثال
WarningHandler.notifyStudentWarning(student, {
  type: 'first',
  reason: 'تكرار الغياب'
});
```

---

## ⚙️ الفئة الإدارية (Admin)

### 19. `admin_action` - تعيين حلقة للمعلم
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تعيين حلقة جديدة للمعلم |
| **المستلم** | المعلم |
| **Handler** | `AdminHandler.notifyGroupAssigned()` |
| **العنوان** | `حلقة جديدة` |
| **الرسالة** | `تم تعيينك على حلقة {اسم الحلقة}` |

---

### 20. `system_update` - تحديث حلقة
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تحديث بيانات الحلقة |
| **المستلم** | معلم الحلقة |
| **Handler** | `AdminHandler.notifyGroupUpdated()` |
| **العنوان** | `تحديث الحلقة` |

---

### 21. `admin_action` - نقل حلقة (من)
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند نقل حلقة من معلم لآخر |
| **المستلم** | المعلم القديم |
| **Handler** | `AdminHandler.notifyGroupTransferredFrom()` |
| **العنوان** | `نقل الحلقة` |
| **الأولوية** | `high` |

---

### 22. `admin_action` - نقل حلقة (إلى)
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند استلام حلقة منقولة |
| **المستلم** | المعلم الجديد |
| **Handler** | `AdminHandler.notifyGroupTransferredTo()` |
| **العنوان** | `حلقة جديدة` |

---

### 23. `alert` - حذف حلقة
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند حذف حلقة |
| **المستلم** | معلم الحلقة + طلاب الحلقة |
| **Handler** | `AdminHandler.notifyGroupDeleted()` + `notifyGroupDeletedForStudents()` |
| **العنوان** | `حذف الحلقة` |

---

### 24. `system` - تحديث بيانات المعلم
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تحديث بيانات المعلم الشخصية |
| **المستلم** | المعلم |
| **Handler** | `AdminHandler.notifyTeacherInfoUpdated()` |
| **العنوان** | `تحديث الملف` |

---

### 25. `success`/`alert` - تغيير معلم الحلقة
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تعيين أو إزالة معلم من حلقة |
| **المستلم** | جميع طلاب الحلقة |
| **Handler** | `AdminHandler.notifyGroupStudentsTeacherChanged()` |
| **العنوان** | `معلم جديد` أو `تغيير المعلم` |

---

### 26. `system` - تغيير اسم الحلقة
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تغيير اسم الحلقة |
| **المستلم** | معلم الحلقة + جميع الطلاب |
| **Handler** | `AdminHandler.notifyGroupRenamed()` |
| **العنوان** | `تغيير الاسم` |

---

### 27. `success` - إضافة طالب (بواسطة الأدمن)
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند إضافة طالب للحلقة بواسطة الأدمن |
| **المستلم** | معلم الحلقة + الطالب المضاف |
| **Handler** | `AdminHandler.notifyAdminAddedStudent()` |
| **العنوان (للمعلم)** | `طالب جديد` |
| **العنوان (للطالب)** | `إضافة للحلقة` |

---

### 28. `warning` - إزالة طالب (بواسطة الأدمن)
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند إزالة طالب من الحلقة بواسطة الأدمن |
| **المستلم** | معلم الحلقة |
| **Handler** | `AdminHandler.notifyAdminRemovedStudent()` |
| **العنوان** | `حذف طالب` |

---

### 29. `warning`/`success`/`system` - نقل طالب (بواسطة الأدمن)
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند نقل طالب بين حلقتين بواسطة الأدمن |
| **المستلم** | المعلم القديم + المعلم الجديد + الطالب |
| **Handler** | `AdminHandler.notifyAdminMovedStudent()` |

| المستلم | العنوان | النوع |
|---------|---------|-------|
| المعلم القديم | `نقل طالب` | warning |
| المعلم الجديد | `طالب جديد` | success |
| الطالب | `تغيير الحلقة` | system |

---

## ⭐ الفئة الأخرى (Other)

### 30. `prayer_time` - وقت الصلاة
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند حلول وقت الصلاة |
| **المستلم** | جميع المستخدمين |
| **ملاحظة** | يُعالج في Frontend عبر `usePrayerAlerts` hook |

---

### 31. `goal` - هدف
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تحقيق هدف أو تعيين هدف جديد |
| **ملاحظة** | Stub - لم يُفعّل بعد |

---

### 32. `achievement` - إنجاز
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تحقيق إنجاز |
| **ملاحظة** | Stub - لم يُفعّل بعد |

---

### 33. `points` - نقاط
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند كسب نقاط |
| **ملاحظة** | Stub - لم يُفعّل بعد |

---

### 34. `ranking` - ترتيب
| الحقل | القيمة |
|-------|--------|
| **متى يُرسل** | عند تغيير الترتيب |
| **ملاحظة** | Stub - لم يُفعّل بعد |

---

## 📊 ملخص الإرسال حسب المستلم

### 👨‍🎓 الطالب يستلم:
| الإشعار | السبب |
|---------|-------|
| غياب | رصد غياب |
| تعديل حضور | تحويل غياب لحضور |
| مقطع جديد/تعديل/حذف | تغييرات في المقاطع اليومية |
| موعد جديد/تعديل/إلغاء | تغييرات في الجدول |
| تنبيه/إنذار | إصدار تحذير |
| رسالة | استلام رسالة |
| منشور | نشر خبر |
| تغيير معلم | تعيين/إزالة معلم |
| إضافة للحلقة | إضافة بواسطة الأدمن |
| تغيير الحلقة | نقل بين حلقتين |
| حذف الحلقة | حذف الحلقة |

### 👨‍🏫 المعلم يستلم:
| الإشعار | السبب |
|---------|-------|
| حلقة جديدة | تعيين حلقة |
| تحديث الحلقة | تعديل بيانات الحلقة |
| نقل الحلقة | نقل الحلقة منه |
| حذف الحلقة | حذف الحلقة |
| طالب جديد | إضافة طالب |
| حذف طالب | إزالة طالب |
| نقل طالب | نقل طالب |
| رسالة | استلام رسالة |
| منشور | نشر خبر |
| تحديث الملف | تعديل بياناته |

---

## 🔧 طريقة الإرسال

كل إشعار يمر بـ 3 قنوات:

1. **💾 Database** - حفظ في MongoDB
2. **📡 Socket.IO** - إرسال فوري للمتصلين
3. **📲 Push (FCM)** - إشعار للأجهزة المحمولة

```javascript
// في NotificationManager.createNotification()
const notification = new Notification(data);
await notification.save();           // 1. Database
await sendRealTimeNotification(io, notification);  // 2. Socket
await sendPushNotification(recipient, notification);  // 3. FCM
```

---

## 📁 هيكل الملفات

```
Backend/src/Notifications/
├── Core/
│   ├── NotificationManager.js   # إدارة الإشعارات
│   ├── SocketSender.js          # إرسال Socket.IO
│   └── PushSender.js            # إرسال FCM
├── Handlers/
│   ├── AdminHandler.js          # إشعارات الأدمن
│   ├── AttendanceHandler.js     # إشعارات الحضور
│   ├── ChatHandler.js           # إشعارات الرسائل
│   ├── DailyMarksHandler.js     # إشعارات العلامات
│   ├── GeneralHandler.js        # إشعارات عامة
│   ├── NewsHandler.js           # إشعارات الأخبار
│   ├── StudentHandler.js        # إشعارات الطلاب
│   ├── TimetableHandler.js      # إشعارات الجدول
│   ├── WarningHandler.js        # إشعارات التحذيرات
│   ├── TeacherHandler.js        # (فارغ - للمستقبل)
│   └── ExamHandler.js           # (فارغ - للمستقبل)
└── index.js                     # تصدير الموديولات
```

---

## 🎨 الأيقونات والألوان

| النوع | الأيقونة | اللون |
|-------|----------|-------|
| message | MessageCircle | sky |
| news | Megaphone | cyan |
| attendance | ClipboardList | amber/orange |
| daily_marks | ClipboardCheck | teal |
| timetable | Calendar | indigo/purple |
| warning | AlertTriangle | amber |
| student_update | GraduationCap | cyan/sky |
| admin_action | Shield | slate |
| success | CheckCircle2 | emerald |
| alert | AlertCircle | rose |

---

## 📌 ملاحظات مهمة

1. **كل طالب يحصل على إشعار منفصل** - لو الحلقة فيها 30 طالب، يتم إنشاء 30 record في DB
2. **الإشعارات تنتهي تلقائياً** - TTL = 90 يوم
3. **الحد الأقصى للإشعارات غير المقروءة** - 100 إشعار للمستخدم الواحد
4. **الأولويات**: `low`, `medium`, `high`, `urgent`

---

*آخر تحديث: يناير 2026*
