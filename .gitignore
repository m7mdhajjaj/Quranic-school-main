# Prevent accidental Windows device filenames that break Git
nul
NUL

# Environment files - NEVER commit these!
.env
.env.local
.env.*.local
**/.env
**/.env.local

# Firebase Service Account - SENSITIVE! NEVER COMMIT!
**/firebase-service-account.json
**/*firebase-adminsdk*.json
