# 🔍 كيف تتحقق من كلمة مرور الطالب

## المشكلة الحالية

من الصورة، الطالب موجود برقم **134886** لكن كلمة المرور خاطئة.

---

## ✅ الحل السريع: تحقق من كلمة المرور في قاعدة البيانات

### الطريقة 1: استخدام السكريبت الجاهز

1. **افتح Terminal في مجلد Backend:**

   ```bash
   cd Backend
   ```

2. **عدّل السكريبت:**
   افتح ملف `checkStudentPassword.js` وغيّر:

   ```javascript
   const STUDENT_ID = 134886; // رقم الطالب من الصورة
   const ID_NUMBER_TO_TEST = "ضع_رقم_الهوية_هنا"; // رقم الهوية الذي تحاول الدخول به
   ```

3. **شغل السكريبت:**

   ```bash
   node checkStudentPassword.js
   ```

4. **راقب النتيجة** - سيخبرك بالضبط:
   - ✅ هل كلمة المرور مشفرة؟
   - ✅ ما هو رقم الهوية الصحيح؟
   - ✅ هل كلمة المرور المُدخلة صحيحة؟

---

## الطريقة 2: استخدام MongoDB Compass

### أ) افتح MongoDB Compass

### ب) اتصل بـ: `mongodb://localhost:27017`

### ج) افتح Database: `quranic_school`

### د) افتح Collection: `students`

### هـ) ابحث عن الطالب:

```json
{ "studentId": 134886 }
```

### و) تحقق من الحقول:

```json
{
  "studentId": 134886,
  "idNumber": "??????????", // <-- هذا رقم الهوية الصحيح
  "password": "$2b$10$...", // <-- يجب أن يبدأ بـ $2b$ أو $2a$
  "firstName": "...",
  "lastName": "..."
}
```

---

## 🎯 القاعدة الذهبية

### عند إنشاء طالب جديد:

```
كلمة المرور = رقم الهوية (idNumber)
```

### عند تسجيل الدخول:

```
- رقم المستخدم = studentId
- كلمة المرور = idNumber (نفس رقم الهوية المستخدم عند الإنشاء)
```

---

## 📋 مثال عملي

### عند إنشاء الطالب:

```
الاسم: محمد أحمد
رقم الهوية: 123456789  <--- هذا مهم! احفظه!
```

↓ النظام يحفظ تلقائياً:

```
studentId: 134886 (يُنشأ تلقائياً)
idNumber: 123456789
password: $2b$10$abcdefg... (رقم الهوية مشفر)
```

### عند تسجيل الدخول:

```
رقم المستخدم: 134886 (رقم الطالب)
كلمة المرور: 123456789 (رقم الهوية - نفس الرقم!)
```

---

## ❌ الأخطاء الشائعة

### 1. استخدام رقم هوية مختلف

```
❌ عند الإنشاء: 123456789
❌ عند الدخول: 987654321  <-- خطأ!
✅ عند الدخول: 123456789  <-- صح!
```

### 2. وجود مسافات

```
❌ " 123456789" أو "123456789 "  <-- فيه مسافات
✅ "123456789"  <-- صح، بدون مسافات
```

### 3. أقل أو أكثر من 9 أرقام

```
❌ "12345678" (8 أرقام)
❌ "1234567890" (10 أرقام)
✅ "123456789" (9 أرقام بالضبط)
```

---

## 🔧 الحل النهائي

### إذا نسيت رقم الهوية:

1. **استخدم MongoDB Compass** أو **السكريبت أعلاه**
2. **ابحث عن الطالب برقم:** 134886
3. **اقرأ حقل `idNumber`** - هذا هو رقم الهوية الصحيح
4. **استخدمه لتسجيل الدخول**

### إذا كانت كلمة المرور غير مشفرة:

سجل دخول كـ Admin واستخدم endpoint التشفير:

```javascript
// في Developer Console (F12)
fetch("http://localhost:5000/api/auth/encrypt-student-passwords", {
  method: "POST",
  headers: {
    Authorization: "Bearer " + localStorage.getItem("token"),
    "Content-Type": "application/json",
  },
})
  .then((res) => res.json())
  .then((data) => console.log("✅", data));
```

---

## 💡 نصيحة

**احتفظ بملف Excel أو Notes** مع معلومات الطلاب:

| رقم الطالب | الاسم     | رقم الهوية | ملاحظات   |
| ---------- | --------- | ---------- | --------- |
| 134886     | محمد أحمد | 123456789  | طالب جديد |
| 134887     | علي حسن   | 987654321  |           |

هكذا لن تنسى أبداً! 📝

---

## 🎯 خلاصة سريعة

```
✅ كلمة مرور الطالب = رقم الهوية
✅ استخدم السكريبت للتحقق من كلمة المرور في DB
✅ تأكد أن رقم الهوية 9 أرقام بالضبط
✅ تأكد من عدم وجود مسافات
```

---

**شغل السكريبت الآن وأخبرني بالنتيجة!** 🚀
